import { apple, deezer, google, spotify } from '../../assets/base/icons'

export const Streaming = () => {
  return (
    <div>
      <a href="#">{apple}</a>
      <a href="#">{deezer}</a>
      <a href="#">{google}</a>
      <a href="#">{spotify}</a>
    </div>
  )
}